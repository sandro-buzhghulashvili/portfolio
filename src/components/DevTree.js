import React from 'react';

import classes from './DevTree.module.scss';

const DevTree = () => {
  return (
    <div className={classes.dev}>
      <h1>This is dev tree</h1>
    </div>
  );
};

export default DevTree;
